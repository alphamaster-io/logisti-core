steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=apps/web/Dockerfile
      - --tag=$_IMAGE
      - .
images:
  - $_IMAGE
options:
  machineType: E2_HIGHCPU_8
  logging: GCS_ONLY
logsBucket: gs://logisti-core-build-logs
timeout: 1800s
